services:
  floorball:
    build: .
    container_name: floorball-stats-tracker
    ports:
      - "5000:5000"
    volumes:
      # Persist the SQLite database and any backup files
      - ./gamesFiles/:/app/gamesFiles/
      - ./logs/:/app/logs/
    env_file:
      - .env
    environment:
      - FLASK_APP=app.py
      - FLASK_RUN_HOST=0.0.0.0
    restart: unless-stopped
